/**
 * Check if a given object implements the InstitutionalHolderItem interface.
 */
export function instanceOfInstitutionalHolderItem(value) {
    if (!("entityName" in value) || value["entityName"] === undefined)
        return false;
    return true;
}
export function InstitutionalHolderItemFromJSON(json) {
    return InstitutionalHolderItemFromJSONTyped(json, false);
}
export function InstitutionalHolderItemFromJSONTyped(json, ignoreDiscriminator) {
    if (json == null) {
        return json;
    }
    return {
        entityName: json["entity_name"],
        dateReported: json["date_reported"] == null ? undefined : json["date_reported"],
        shares: json["shares"] == null ? undefined : json["shares"],
        value: json["value"] == null ? undefined : json["value"],
        percentHeld: json["percent_held"] == null ? undefined : json["percent_held"],
    };
}
export function InstitutionalHolderItemToJSON(json) {
    return InstitutionalHolderItemToJSONTyped(json, false);
}
export function InstitutionalHolderItemToJSONTyped(value, ignoreDiscriminator = false) {
    if (value == null) {
        return value;
    }
    return {
        entity_name: value["entityName"],
        date_reported: value["dateReported"],
        shares: value["shares"],
        value: value["value"],
        percent_held: value["percentHeld"],
    };
}
